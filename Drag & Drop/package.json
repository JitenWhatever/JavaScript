{
  "name": "drag-and-drop",
  "version": "1.0.0",
  "description": "Drag & Drop",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile:tsc": "tsc",
    "devserver": "live-server",
    "start": "npm-run-all --parallel devserver compile:tsc"
  },
  "keywords": [
    "typescript",
    "course"
  ],
  "author": "Jiten",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
